// Nothing required to run the site — this file is here so you have a
// place to add interaction later (e.g. filtering work by category,
// a lightbox for project images, etc).

document.addEventListener('DOMContentLoaded', () => {
  console.log('Portfolio loaded.');
});
